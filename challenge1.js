// Define a function called metersToInches that takes an input of the number of
// meters and outputs the result of the number of inches.

function metersToInches(numMeters){
    let inches = numMeters*(39.3701);
    return inches
}

console.log(numMeters(3));

